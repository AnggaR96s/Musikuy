<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.angga.dicoding.musikuy">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.angga.dicoding.musikuy.Details"></activity>
        <activity android:name="com.angga.dicoding.musikuy.ActivityAbout" />
        <activity android:name="com.angga.dicoding.musikuy.ListActivity" />
        <activity android:name="com.angga.dicoding.musikuy.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>